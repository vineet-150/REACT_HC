import Chai from './chai.js';

function App() {
  return (
    <>
      <h1>Learing Day 02 | Vineet Kumar </h1>
   <Chai/>
</>
  );

}

export default App;// exporting the function


// concept of react-> React is give the intresting syntax that is take the function and return the function in the HTML then export this function and render this HTML by the help of function 

// Correct Concept

// React me:

// Hum function component banate hain

// Wo function JSX (HTML jaisa syntax) return karta hai

// Us component ko export karte hain

// Fir React us component ko DOM me render karta hai


// we are writing the html through JS -> fadya kaya ha (our profit is that the programming capability is added in HTML its for the complex UI)
